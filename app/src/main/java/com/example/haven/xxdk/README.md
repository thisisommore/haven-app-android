Android mirror of the iOS `XXDK` area.

This is intentionally lightweight for now:
- package layout mirrors the iOS structure
- password and codename flows are wired against the real Android bindings
- SQLite-backed messaging setup is intentionally deferred for now
- current UI can adopt these classes incrementally

Files with `+` in the iOS tree are mirrored with the same naming style here.

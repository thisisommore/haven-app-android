package com.example.haven.ui.theme

import androidx.compose.ui.graphics.Color

// Material 3 tonal palette based on a single seed color
// These are auto-generated using Material Theme Builder or use Material defaults

// If you want to use Material 3 dynamic colors (Android 12+), you don't need this file
// Just use dynamicDarkColorScheme(context) / dynamicLightColorScheme(context) in Theme.kt

// Keep this file minimal - only define colors that are truly app-specific
// For everything else, use MaterialTheme.colorScheme
